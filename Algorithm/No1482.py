# run time: 69%
# memory: 75%
class Solution:
    def action(self, bloomDay: [], m: int, k: int):
        # day = min(bloomDay)
        # for i in range(len(bloomDay)):
        #     if bloomDay[i] == 12:
        #         bloomDay.pop(i)
        # day = -1
        # len_bloomDay = len(bloomDay)
        # while m > 0 and len_bloomDay > 0:
        #     for i in range
        #
        #
        # return bloomDay

    def leet_code(self, nums: [], n: int):
        return 1


solution = Solution()
# print(solution.action([1,10,3,10,2], 3,1))
# print(solution.action([1,10,3,10,2], 3, 2))
print(solution.action([7, 7, 7, 7, 12, 7, 7], 2, 3))

# code toi uu
